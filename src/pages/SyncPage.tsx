export default function SyncPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">IDE 同步</h1>
      <p className="text-[hsl(var(--muted-foreground))]">
        将 Skill 同步到 IDE 项目目录
      </p>
    </div>
  );
}
